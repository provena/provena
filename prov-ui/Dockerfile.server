# On-prem server mode. Build from repo root: docker build -f prov-ui/Dockerfile.server .
FROM node:18-alpine
WORKDIR /provena
COPY . .
WORKDIR /provena/prov-ui
RUN npm install && npm run build:example
EXPOSE 3000
CMD ["npm", "run", "serve"]
